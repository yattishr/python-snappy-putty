You will implement **M3 — Snappy Agent Loop v1** in **three sequential chunks**, using the instruction files already present in the repo:

- `CHUNK1.md`
- `CHUNK2.md`
- `CHUNK3.md`

## Execution order

You must execute them in this exact order:

1. `CHUNK1.md`
2. `CHUNK2.md`
3. `CHUNK3.md`

Do **not** skip ahead.
Do **not** merge all three into one patch.
Do **not** begin the next chunk until the current chunk is completed, reviewed, and summarized.

---

## Operating rules

For each chunk:

1. Read the chunk instruction file carefully
2. Implement **only** that chunk’s scope
3. Do not add features outside that chunk, even if they seem related
4. Keep changes bounded and milestone-scoped
5. Reuse existing architecture where possible
6. Avoid duplicate lifecycle/state systems
7. Run or update relevant tests for that chunk
8. Produce a short summary before moving to the next chunk

---

## Output required after each chunk

After completing each chunk, return:

### A. Chunk completion header
Example:
```text
Completed CHUNK1.md
```
